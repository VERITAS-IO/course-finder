# APILER

## YOUTUBE API
1. Arama sonuclarinda, playlist id dondugu zaman, o playlist bilgielrini ekstradan getirmeliyiz.

## JSON PARSE
1. Bazen, sadece json gelmek yerine, responseda bir text aciklama geliyor. Aciklama........```json diye geliyor. Bu nedenle json parse hatasi aliyor.

## PROMPT

1. Bir ve ikinci openai requestlerindeki promptlar cok optimize olmali.
    1.requestte istedigimiz structure'da bir function_call response uretilmeli.
    2.ikinci requestte istedimigiz structureda bir response objesi uretilmeli.
    3.iki requestte de ekstra bir aciklama olmamali.JSON parse ederken hata aliyor.